server:
  port: 8083

spring:
  application:
    name: jobseeker-service

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/dreamjob
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
